# Hosted Sites uses dispatch-owned login. Self-hosted Workers must set AUTH_MODE=token.
AUTH_MODE=token
ADMIN_TOKEN=
CRON_TOKEN=
# AI / Telegram / IPinfo keys are configured through the authenticated settings UI.
