import {CSSProperties} from 'react';
export function SegmentBar({value,color='purple',threshold=100,label='使用率',count=28}:{value:number;color?:string;threshold?:number;label?:string;count?:number}){const n=Math.min(100,Math.max(0,Number.isFinite(value)?value:0));return <div className={`segment-meter meter-${value>=threshold?'red':color}`} style={{'--segment-count':count} as CSSProperties} role="progressbar" aria-label={label} aria-valuenow={Number(n.toFixed(2))} aria-valuemin={0} aria-valuemax={100}>{Array.from({length:count},(_,i)=><i key={i}><span style={{width:Math.max(0,Math.min(100,n*count-i*100))+'%'}}/></i>)}</div>}
